name: YouTube Otomatik Güncelleyici

on:
  schedule:
    - cron: '0 */6 * * *' # Her 6 saatte bir çalışır
  push:
    branches:
      - main
    paths:
      - 'link.json'
      - 'm3u8.sh' # Script değişirse de tetiklensin
  workflow_dispatch:

jobs:
  build-and-push-playlist:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Depoyu Klonla
        uses: actions/checkout@v4

      - name: Gerekli araçları kur (jq)
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Playlist Oluşturma Script'ini Çalıştır
        run: |
          chmod +x m3u8.sh
          ./m3u8.sh

      - name: Değişiklikleri Depoya Yükle
        run: |
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          # Hem playlist klasörünü hem de kök dizindeki ana m3u'yu ekle
          git add playlist/ "$MAIN_PLAYLIST_FILE"
          
          if ! git diff-index --quiet HEAD; then
            git commit -m "✅ TV Listesi Otomatik Güncellendi: $(date)"
            git pull --rebase
            git push
          else
            echo "🔄 Değişiklik yok, yükleme atlandı."
          fi
